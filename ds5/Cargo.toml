[package]
name = "jaka-ds5-control"
version = "0.1.0"
edition = "2024"

# This package is a pure gamepad to TCP protocol layer, it never links the
# JAKA SDK. It talks to the serve protocol of jaka-cli over TCP
[dependencies]
